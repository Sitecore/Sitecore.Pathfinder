{
  // wwwroot points to the base directory of the website
  // The wwwroot directory must contain a 'Data' and a 'Website' folder
  "wwwroot": "e:\\inetpub\\wwwroot\\Pathfinder",
  "hostname": "http://pathfinder",

  // relative project directory to solution directory
  "projectdirectory": "",

  // relative directory to output directory
  // the content directory must start with 'content'
  "contentdirectory": "content/project",

  // relative directory to output directory
  // the serialization directory must start with 'serialization' followed by the database name
  "serializationdirectory": "serialization/master/sitecore",
  "database": "master",

  // build pipeline
  "pipeline": "clean incremental-build create-nuspec create-nupkg"
}
                                       